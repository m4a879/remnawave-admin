"""Эталонная (актуальная) версия node-agent для сравнения в панели.

Агент репортит свою версию в каждом батче (node-agent/src/version.py,
AGENT_VERSION) — панель сравнивает её с LATEST_AGENT_VERSION и подсказывает,
что агент пора обновить. При релизе новой версии агента поднимать ОБА
значения синхронно.
"""

LATEST_AGENT_VERSION = "1.1.0"
